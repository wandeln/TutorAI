# Compute-Agent (eigener, schlanker Service)
# Dependencies bewusst klein: docker-CLI wird per subprocess verwendet,
# Asset-Downloads per urllib (Bibliotheken).

fastapi==0.115.6
uvicorn[standard]==0.34.0
pyyaml==6.0.2
